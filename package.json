{
  "name": "guardian-companion-spectrum",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node dist/index.js && echo 'Simulation test complete'",
    "lint": "echo 'Add ESLint if desired'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
