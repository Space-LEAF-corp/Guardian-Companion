{
  "name": "guardian-companion-spectrum",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "services/*"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node dist/index.js && echo 'Simulation test complete'",
    "test:unit": "tsc && node dist/tests/unit/run-tests.js",
    "lint": "echo 'Add ESLint if desired'",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^5.0.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
